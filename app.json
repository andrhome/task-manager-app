{
  "environments": {
    "test": {
      "formation": {
        "test": {
          "quantity": 1,
          "size": "standard-1x"
        }
      }
    }
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "env-cmd --file ./config/dev.env nodemon src/index.js",
    "test": "env-cmd --file ./config/test.env jest"
  }
}
